| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| `build_jp_policy` complexity finding | Isolate the policy derived independently for each registered JP tag | The mapping from each validated JP tag record to its restrictions, translation exception, special translation action, and copy permission | Means | registered JP tag policies, then source-tag omission policies, then policy document | _build_jp_tag_policies | `_build_jp_tag_policies` refers to deriving target-tag policy records from validated JP tags. |
| `build_jp_policy` complexity finding | Isolate the source-language tags that translations must omit | The per-branch source-tag policies derived from unreplaced JP-unused tags and unmapped EN genre categories | Means | registered JP tag policies, then source-tag omission policies, then policy document | _build_source_tag_policies | `_build_source_tag_policies` refers to deriving per-branch omission policies for source-language tags. |
| `parse_overrides` complexity finding | Keep one override value's shape and target validation independent of collection traversal | The current JP tag named by one valid string or `{ "jp_tag": ... }` branch-override value | Value | validate branch and source-tag keys, validate one target value, then add the override | _validated_override_target | `_validated_override_target` refers to extracting and validating the current JP target from one branch-override value. |
