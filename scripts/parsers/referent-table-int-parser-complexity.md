| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| INT parser complexity finding | Separate branch/source expansion from row parsing and target resolution | The ordered `(branch, source_tag)` pairs derived from every recognized source column in one complete INT table row | Value | select a complete row, expand source columns into branch/tag pairs, then yield resolver candidates | _iter_source_tag_branches | `_iter_source_tag_branches` refers to iterating the branch and source-tag pairs declared by one complete INT crosswalk row. |
| INT row-contract regression | Preserve positional header selection and short-row tolerance during extraction | The condition that reordered source columns resolve correctly while rows shorter than the active header produce no candidates | State | parse a reordered header, accept one complete row, skip one short row, then inspect mappings | test_int_crosswalk_respects_reordered_columns_and_skips_short_rows | `test_int_crosswalk_respects_reordered_columns_and_skips_short_rows` refers to verifying reordered-column selection and short-row rejection in the INT parser. |
| INT conflict regression | Preserve candidate reduction when one source tag points at different JP targets | The condition that repeated occurrences of one branch source tag with conflicting JP cells are omitted from the final crosswalk | State | parse repeated source tags, resolve distinct targets, then inspect the omitted mapping | test_int_crosswalk_omits_repeated_source_with_conflicting_targets | `test_int_crosswalk_omits_repeated_source_with_conflicting_targets` refers to verifying that conflicting repeated INT candidates are excluded. |
