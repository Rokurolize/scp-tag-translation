| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| CN branch-guide parser complexity finding | Make one CN list item independently decidable | The optional source tag, EN tag candidates, and JP tag candidates extracted from one CN guide line | Value | select local link, validate source, collect official targets, then try parenthetical EN fallback | _parse_cn_line | `_parse_cn_line` refers to extracting one optional mapping row from one CN guide line. |
| CN branch-guide parser complexity finding | Reuse ordered host filtering for linked target candidates | The ordered paths from parsed tag links whose host contains one of the supplied official host fragments | Value | parse links, select paths by official host, then resolve the row | _paths_matching_hosts | `_paths_matching_hosts` refers to selecting linked tag paths whose hosts match the supplied official host fragments. |
| Branch-guide analysis complexity finding | Keep conflict and audit accounting local to one branch | The accepted source-to-JP mappings and exact audit counts obtained by resolving all parsed rows for one branch | Record | parse branch rows, resolve candidates, exclude unresolved or conflicting sources, then count outcomes | _analyze_branch_rows | `_analyze_branch_rows` refers to resolving and auditing all parsed guide rows for one branch. |
| Crosswalk resolver initialization complexity finding | Isolate construction of the three indexes over current JP tags | The exact JP names, normalized JP-name lookup, and normalized source-alias lookup derived from validated current JP tags | Record | validate records, index current JP tags, then index EN replacements | _index_current_tags | `_index_current_tags` refers to populating the resolver indexes derived from current JP tags. |
| Crosswalk resolver initialization complexity finding | Isolate deprecated and semantic EN replacement ingestion | The normalized EN source tags that deterministically replace to one current JP tag | Record | validate records, index current JP tags, then index EN replacements | _index_en_replacements | `_index_en_replacements` refers to populating deterministic EN replacement mappings. |
| JP parser complexity finding | Separate one registered definition line from cross-line merging | The zero or more JP tag records declared on one uncommented registered-fragment line, sharing its restriction prefix and description | Value | parse registered line entries, then merge duplicate tag names across lines and fragments | _registered_tag_entries | `_registered_tag_entries` refers to the JP tag records declared by one registered definition line. |
| JP parser complexity finding | Preserve duplicate-name merge rules behind one operation | The update that adds a new JP tag record or combines its description, restrictions, translation exception, and unique source aliases with an existing record | Event | parse registered line entries, then merge duplicate tag names across lines and fragments | _merge_jp_tag | `_merge_jp_tag` refers to adding or merging one parsed JP tag record by canonical name. |
